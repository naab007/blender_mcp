# Hand-off: blender_mcp run of 2026-09-11 (team nuzukm)

Status: written 2026-09-11 on branch `blender-5.2` (uncommitted, like everything else in this
run) after the user's directive to complete B1 and deploy for live testing by another agent. The Lead deploys (section 2), another agent runs the live tests (section 3, `docs/LIVE-TEST-PLAN.md`),
the user decides the git switch and release (section 4). Agents built and tested headless only.

## 1. What this run produced

| Version | Phase | Content | Frozen hashes (git hash-object) | Headless evidence |
|---|---|---|---|---|
| 2.0.0 | A: Blender 5.2 LTS migration | five 5.x breaks fixed (EEVEE id, `Scene.node_tree`, boolean `FAST`->`FLOAT`+`MANIFOLD`, `media_type`, `ShaderNodeSeparateRGB`), HDRI temp leak, extrude cap, active/selection restore, portable-folder resolver | addon `e0329ced`, server `c080dfeb`, `__init__` `01a8fd68` | 134/134 on 4.3.2 and 5.2.1 |
| 2.1.0 | B0: settings, save and load | 56 new tools, autostart, prefs move, settings file, presets, session state, `PROTOCOL = 1` | addon `81038008`, server `598a765d`, `__init__` `3410b362`, `settings.py` `2bbc200b`, `presets_default.json` `0a58e8c3` | 165/165 both; venv 49/49; baseline 148 names |
| 2.2.0 (+ A1.2) | B1: rigging and animation | rig-aware `export_object` / `add_keyframe` / capture overlays / `parent_object` / `import_file` / `get_object_info`; 17 rigging tools (armatures, bones, skinning, weights, pose, constraints; `find_unweighted_vertices(render=True)` returns an image); `set_keyframes`, `get_animation_info`, `playblast`, `bake_action` | addon `0eb7ec4c` (B1-A L1 c51a5b68, L2 339e66f3, L3 01d0e4c6, L3.1 K8 = 37d2211d, A1.2 f4dc37b1 = the live agent's five GUI fixes, A1.3 = select flags restored), server `6f249049` (L1 44a80bc6, L1.1+L2+L3 a3b579aa, L3.1), `__init__` `f76c47d1`, `settings.py` `2bbc200b`, `presets_default.json` `0a58e8c3` | headless 186/186 on 4.3.2 and 186/186 on 5.2.1 at the final pair (addon 0eb7ec4c / server 2ddfc98a), venv 69/69; A1.2 re-runs 185/185 both (`tests/report_a12_*.json`); baseline `tests/baseline_tools_2.2.txt` = 169 names; LIVE PASS on 5.2.1 GUI (`docs/LIVE-TEST-REPORT-2026-09-11.md`): every step PASS except L7.3 (needs a human click) and L13 (Rigify, skipped by ruling); F6 open |

The working tree on `blender-5.2` holds all three versions at once (the version files read
2.2.0). No commit has been made in this run; `main` still points at v1.6.0 (`ef0ff7d`).
Headless evidence at the frozen pair (B1-G3, 2026-09-11 14:01): harness 185/185 on 4.3.2 and
185/185 on 5.2.1 (Phase A 133 + B0 31 + B1 21 steps, 158/158 handlers), venv 64/64,
`tests/baseline_tools_2.2.txt` = 169 names.

## 2. Deploy: six files + the add-on, FOUR live targets (Lead copies after B1-G3)

**DEPLOYED 2026-09-11 14:01 by the Lead (copy only, every file re-hashed at the target =
source; nothing committed), then addon.py re-deployed by the live-test agent as A1.2 (`f4dc37b1`,
verified at all four targets by the Lead and the Verifier, headless 185/185 both installs):**

| Target | State after the copy |
|---|---|
| `B:\-AI-Stuff-\-=MCP-Servers=-\blender_mcp\` (the server the Claude Code MCP config launches) | addon.py `f4dc37b1` (A1.2; was 37d2211d at 14:01), pyproject `0197888a`, `src/blender_mcp/server.py` `6f249049`, `__init__.py` `f76c47d1`, `settings.py` `2bbc200b`, `presets_default.json` `0a58e8c3`, plus TOOLS.md and README.md; its venv imports blender_mcp 2.2.0 / PROTOCOL 1 / 169 tools |
| `D:\blender-4.3.2-windows-x64\MCP\` | the same eight files (addon.py now `f4dc37b1`); its venv imports 2.2.0 / 169 tools |
| `%APPDATA%\Blender Foundation\Blender\4.3\scripts\addons\addon.py` | `f4dc37b1` (A1.2), `__pycache__` cleared |
| `%APPDATA%\Blender Foundation\Blender\5.2\scripts\addons\addon.py` | `f4dc37b1` (A1.2; folder created at 14:01) |

The live-testing agent needs a NEW Claude Code session (the MCP server process must restart
to load 2.2.0) and a fresh Blender launch (5.2.1: enable "Blender MCP" once in Preferences;
autostart then opens `127.0.0.1:9876`). Its script is `docs/LIVE-TEST-PLAN.md`.


Source: `D:\App Dev\blender_mcp` on branch `blender-5.2`. Copy with `shutil.copy` or Explorer
(robocopy/xcopy silent-fail from bash). Files: `addon.py`, `src/blender_mcp/server.py`,
`src/blender_mcp/__init__.py`, `src/blender_mcp/settings.py`,
`src/blender_mcp/presets_default.json`, `pyproject.toml` (`img_to_3d_server.py` unchanged).

| Target | Receives | After copying |
|---|---|---|
| `B:\-AI-Stuff-\-=MCP-Servers=-\blender_mcp\` (the MCP config's server, THE live registration) | the six files, same relative paths (`src\blender_mcp\...`) | `uv pip install -e .` in its venv; verify `python -c "import blender_mcp.server as s; print(s.__file__)"` prints that folder's `src\` |
| `D:\blender-4.3.2-windows-x64\MCP\` (project-scoped registration used by unity_mcp) | the six files | same install + verify |
| `%APPDATA%\Blender Foundation\Blender\4.3\scripts\addons\addon.py` (existing 4.3 install) | `addon.py` | delete `__pycache__\addon.cpython-311.pyc`; cycle the add-on off/on in 4.3.2 if it is running (the socket server restarts) |
| `%APPDATA%\Blender Foundation\Blender\5.2\scripts\addons\addon.py` (NEW, the folder was empty) | `addon.py` | enable "Interface: Blender MCP" in 5.2.1 Preferences > Add-ons once; autostart is on by default; delete `__pycache__\addon.cpython-313.pyc` on later updates |

One `addon.py` serves both Blender versions (4.x/5.x differences are resolved by try-assign).
API keys live in the add-on Preferences since 2.1.0: enter them once per Blender installation
(a key found in an old `.blend` is migrated on first load). The MCP client session must be
restarted after the server copy so the new tools appear (`get_version` reports server, add-on
and protocol; a `[blender-mcp mismatch]` line means the add-on copy is stale). `start_blender`
picks the highest Blender it finds (5.2.1); set `BLENDER_EXE` or the settings-file
`blender_exe` to pin 4.3.2.

Timbermesh: the plugin is installed for 4.3 only; copy `timbermesh_blender_plugin` into the
5.2 add-ons folder before any Timbermesh export on 5.2.1.

## 3. Live checks, in run order, each block preceded by the deploy it needs

The headless harness cannot see viewport captures, video output, autostart on a GUI launch or
Rigify's generator UI. The live-test agent runs these blocks in this order from
`docs/LIVE-TEST-PLAN.md` and pastes the printed output to the Lead; a defect found live becomes a small follow-up landing on the same owner's file.

| Order | Block | Deploy first | Where written | Steps |
|---|---|---|---|---|
| 1 | M14 (Phase A, 2.0.0 behaviour) | the six files of section 2 + `addon.py` into the 5.2 add-ons folder (the add-on is already 2.2.0; it contains 2.0.0's fixes) | `docs/MIGRATION-blender-5.2.md`, "Live checklist (M14)" | L1 legacy add-on load + socket, L2 PNG still then FFMPEG with `media_type`, L3 viewport tools through MCP (screenshot, angle, contact sheet, depth map, render, EEVEE id, boolean FAST alias), L5 `start_blender` paths, L6 one-way `.blend` |
| 2 | B0-G5 (2.1.0 behaviour) | same deploy (no newer file needed); restart the Claude Code session once | `docs/FEATURE-REQUEST-settings-save-load.md` section 8 | L7 autostart on a cold launch, L8 `start_blender(start_server=True)`, L9 session save / restore across a restart |
| 3 | B1-G5 (2.2.0 behaviour) | same deploy; Rigify enabled for L13 | `docs/LIVE-TEST-PLAN.md` (Grace; numbered calls with PASS conditions) and `docs/PLAN.md` 10.3 | L10 `render_weight_map` non-blank, L11 `playblast` OpenGL path + `video_path` MP4, L12 capture `overlay=bones_in_front`, L13 Rigify generate (confirmation of the headless result) |

Because one add-on file carries all three versions, one deploy serves all three blocks; the
"deploy first" column exists so a later re-deploy after a follow-up fix is done before the
block that needs it.

## 4. Git: what happens now and what is deferred (user directive 14:31, Lead ruling)

NOW (the Lead runs it after the final re-run): ONE commit on `blender-5.2` with everything this
run produced, pushed to `origin/blender-5.2`. NO tag, NO branch switch, `main` stays at
v1.6.0 (`ef0ff7d`), NO deploy (the live copies stay at the A1.2 pair until the user says
deploy; the committed pair differs from the deployed one by F6 on the server and A1.3 on the
add-on). The commit message is `docs/COMMIT-MESSAGE-v2.2.0.md`. `TERMS_AND_CONDITIONS.md` IS included
(Lead ruling 14:36: it is the fork's own terms rewrite, dated September 2026; shipping the
upstream author's privacy policy under a fork that collects nothing would be wrong). The stray
`pb_1..4.png` were Campbell's test artefacts and are removed; nothing is excluded.

```
cd "D:/App Dev/blender_mcp"
git status                                    # review: everything listed is part of the commit
git add -A
git commit -F docs/COMMIT-MESSAGE-v2.2.0.md
git push origin blender-5.2
```

LATER, at release (the user decides; nothing here runs now): the switch `main` -> `blender-4.3`
(archive, keeps v1.6.0), `blender-5.2` -> `main`, ONE tag `v2.2.0` whose release body carries
the 2.0.0 / 2.1.0 / 2.2.0 sections from `docs/release-notes/` (Lead ruling 12:27; per-version
tags are not reproducible because no intermediate commits exist), `addon.py` attached; then
deploy the committed pair to the four targets of section 2.

```
git fetch origin
git branch -m main blender-4.3 && git push origin blender-4.3
git branch -m blender-5.2 main && git push origin main --force-with-lease   # every clone must fetch and reset
gh repo edit naab007/blender_mcp --default-branch main
git tag -a v2.2.0 -m "Blender 5.2 LTS migration, settings/save/load, rigging and animation" && git push origin v2.2.0
gh release create v2.2.0 addon.py --title "v2.2.0 -- Blender 5.2 LTS, settings, rigging" --notes-file docs/release-notes/v2.2.0.md
```

## 5. Where the documentation lives

- `README.md`: install, preferences and API keys, server settings file, env vars,
  troubleshooting (EEVEE id, wrong Blender, boolean solver, mismatch notice).
- `TOOLS.md`: every tool, sections 1-26 (2.0.0), 27-31 (2.1.0), [32-35 + Animation] (2.2.0).
- `/blender` skill (`C:\Users\Naabin\.claude\commands\blender.md`): patterns 1-9 [+10-11].
- `docs/MIGRATION-blender-5.2.md`: the migration record, breaks table with status, M14.
- `docs/PLAN.md`: the run's contract, decisions D1-D29+, deltas C1-C31+, K1-K23, sections 9-11.
- `docs/FEATURE-REQUEST-*.md`: request docs with "verified on 4.3.2 and 5.2.1" facts and
  per-tool switch tables (settings 5.1, rigging 5.1, engine-readiness 4.1).
- `docs/apichecks/`: the check scripts and both saved outputs (the drift baseline).
- `tests/`: harness (`headless_handlers.py` + `b0_steps.py` [+ `b1_steps.py`]), venv suites,
  `baseline_tools_2.1.txt` [+ `2.2`], reports and logs per install.
- Memory: `~/.claude/projects/D--App-Dev/memory/project_blender_mcp.md` session block
  2026-09-11; MemPalace `main` db, wing `d__app_dev`, room `decisions` (migration + B0 [+ B1]).

## 6. Release-notes bodies

The three bodies are in `docs/release-notes/v2.0.0.md`, `v2.1.0.md` and `v2.2.0.md`. For the
single-tag release (section 4) paste them in order into one body under three headings, or
pass `--notes-file` per tag if the user chooses three releases. Each was written from the
accepted landings; nothing in them describes behaviour that was not headless-verified, and
the live-only items (FFMPEG output, viewport captures, autostart on a GUI launch) are worded
as such.

## 7. Open items and facts to remember

- LIVE PASS 2026-09-11 (`docs/LIVE-TEST-REPORT-2026-09-11.md`): all blocks PASS on 5.2.1 GUI
  except L7.3 (autostart from a cold GUI launch needs a human click to relaunch Blender; not
  run) and L13 (Rigify, skipped: Tier 2 not shipped). Five add-on defects found live are
  fixed in A1.2 (addon `f4dc37b1`, ACCEPTED and FINAL 14:27: Ton's diff review CLEAN, +53/-9
  in the five claimed spots, no handler keys changed; headless 185/185 both installs, venv
  64/64). CAVEAT: the five fixes are GUI-only and are "verified live by the live-test agent,
  not reproducible headless". Playblast tiles are now always even-sized (an odd `max_size`
  rounds down by 1). Fixes: F1 `capture_viewport_angle` wrote a 1x1 PNG on 5.2.1 (the DRAW_WIN_SWAP redraw inside the area/region override broke the following `screenshot_area`; redraw moved outside the override, 1x1 guard + retry; also fixed `capture_contact_sheet`, `render_weight_map`, `find_unweighted_vertices(render=True)` and the overlay captures); F2 `playblast(video_path=)` failed on odd dimensions ("height not divisible by 2"): dimensions are made even; F3 `playblast(camera=)` on the OpenGL path drew the viewport's own view: the region is switched to the camera view during capture and restored; F4 `start_blender(restore_session=)` reported "no window in this session" in a GUI session (`bpy.context.window` is None in the timer right after `open_mainfile`): `_gui_window()` falls back to the window manager's first window in `set_workspace` / `list_workspaces`; F5 `find_unweighted_vertices(render=True)` showed stale face selection: edge and polygon flags are cleared too.
- F6 CLOSED (server `2ddfc98a`, landed 14:32 by Campbell, Lead ruling R-F6): `send_command` runs under a class-level RLock, and a transport failure on connect/sendall is retried ONCE by reconnect-and-resend because the request never reached the add-on; a failure during receive is never retried (the add-on may have executed the command). Evidence: tools/list byte-identical before and after (169 = `tests/baseline_tools_2.2.txt`), venv 64/64, mocked sockets: reset before send -> one reconnect + one resend with exactly one request delivered; reset during receive -> no resend. The original observation: one WinError 10054 in a 5-call parallel batch (the add-on closing the socket).
- A1.3 FIXED (addon `0eb7ec4c`, landed by Ton, Lead ruling R-A1.3): `find_unweighted_vertices(render=True)` now restores the mesh's vertex, edge and polygon select flags in `finally` (it used to leave the unweighted vertices selected, since 37d2211d). Reply keys unchanged.
- The live agent edited and deployed `addon.py` itself, outside the one-owner rule; the
  result is verified and stands as A1.2 (Lead 14:26).

- Not started in this run (user's pause directive 12:25): B2 (engine readiness) onward.
  `docs/PLAN.md` section 11 is the B2 plan, marked "planned, not started; start condition
  B1-G3 + user resume"; its Tier 2 split is already ruled (L4 set: reduce_bone_influences,
  check_vertex_count_split, make_single_user, set/get_custom_properties, merge_for_export,
  remesh_object; held: generate_lightmap_uv until B3, export_timbermesh until the plugin is
  installed on 5.2.1). The B2 facts are verified on both installs already (engine-readiness
  doc section 4.1, `apichecks/b2check.py`), so the next run starts at Ton's B2-A L1.
- `TERMS_AND_CONDITIONS.md` was already modified locally before the run started (the fork's
  own terms rewrite); included in the commit by the Lead's ruling of 14:36.
- Unconfirmed on 5.2: "All Libraries / Essentials at asset-library indices 0/1" (headless shows
  only User Library); nothing built on it.
- `recover_file(mode="LAST_SESSION")` reopens the real quit.blend: never from a script.
- 5.x `.blend` files do not open in 4.3; `bpy.data.is_dirty` is False headless on 5.2 after
  edits; `numpy.array(Vector)` is float32 on 5.2 (pass `dtype=float64`).
- Legacy Scene secret properties stay hidden through 2.2.0 and are removed in 2.3.0 (Lead
  ruling 2026-09-11: a 1.6.0 -> 2.2.0 deploy must still migrate them once).
